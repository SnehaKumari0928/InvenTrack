import { useEffect, useState } from "react";
import { ArrowLeft, CheckCircle2, Clock3, PackageCheck } from "lucide-react";
import { Link, useParams } from "react-router-dom";
import { orderApi, getErrorMessage } from "../services/api";
import Loading from "../components/Loading";

const money = new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:2});
export default function OrderDetail(){const {id}=useParams();const [o,setO]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{orderApi.get(id).then(r=>setO(r.data)).catch(e=>setError(getErrorMessage(e))).finally(()=>setLoading(false))},[id]);if(loading)return <Loading/>;if(error)return <div className="rounded-lg bg-red/5 p-4 text-sm text-red">{error}</div>;return <div><Link to="/dashboard" className="inline-flex items-center gap-2 text-sm font-semibold text-slate hover:text-teal"><ArrowLeft size={16}/> Dashboard</Link><div className="mt-5 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-teal">Order #{o.id}</p><h1 className="page-title mt-1">Order confirmation</h1><p className="mt-1 text-sm text-slate">{new Date(o.createdAt).toLocaleString("en-IN")}</p></div><Status status={o.status}/></div><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_320px]"><section className="card overflow-hidden"><div className="border-b border-line px-5 py-4"><h2 className="font-bold">Items</h2></div><div className="divide-y divide-line">{o.items?.map(x=><div key={x.productId} className="flex items-center gap-4 px-5 py-4"><div className="grid h-10 w-10 place-items-center rounded-lg bg-surface text-teal"><PackageCheck size={18}/></div><div className="min-w-0 flex-1"><div className="truncate font-semibold">{x.productName}</div><div className="text-xs text-slate">{x.quantity} × {money.format(x.unitPrice)}</div></div><div className="font-semibold">{money.format(x.totalPrice)}</div></div>)}</div></section><aside className="card p-5"><h2 className="font-bold">Customer</h2><div className="mt-4 space-y-3 text-sm"><div><div className="text-xs text-slate">Name</div><div className="font-semibold">{o.customerName}</div></div><div><div className="text-xs text-slate">Email</div><div>{o.customerEmail||"—"}</div></div><div className="border-t border-line pt-4"><div className="text-xs text-slate">Total</div><div className="mt-1 text-2xl font-bold">{money.format(o.totalAmount)}</div></div></div></aside></div></div>}
function Status({status}){return <span className="inline-flex items-center gap-2 rounded-full bg-teal/10 px-3 py-1.5 text-sm font-bold text-teal"><CheckCircle2 size={16}/>{status}</span>}