import { useEffect, useState } from "react";
import { Shield, UserRound } from "lucide-react";
import { userApi, getErrorMessage } from "../services/api";
import Loading from "../components/Loading";
import EmptyState from "../components/EmptyState";

export default function Users(){const [users,setUsers]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{userApi.list().then(r=>setUsers(r.data)).catch(e=>setError(getErrorMessage(e))).finally(()=>setLoading(false))},[]);return <div><h1 className="page-title">Users</h1><p className="mt-1 text-sm text-slate">Accounts with access to the inventory workspace.</p>{error&&<div className="mt-5 rounded-lg bg-red/5 p-3 text-sm text-red">{error}</div>}<div className="card mt-6 overflow-hidden">{loading?<Loading/>:users.length===0?<EmptyState title="No users found"/>:<div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left"><thead className="border-b border-line bg-surface text-xs uppercase tracking-wide text-slate"><tr><th className="px-5 py-3.5">User</th><th className="px-5 py-3.5">Email</th><th className="px-5 py-3.5">Role</th></tr></thead><tbody className="divide-y divide-line">{users.map(u=>{const admin=u.role===1||u.role==="Admin";return <tr key={u.id}><td className="px-5 py-4"><div className="flex items-center gap-3"><div className="rounded-full bg-surface p-2 text-slate"><UserRound size={17}/></div><div className="font-semibold">{u.username}</div></div></td><td className="px-5 py-4 text-sm text-slate">{u.email}</td><td className="px-5 py-4"><span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-bold ${admin?"bg-navy/10 text-navy":"bg-teal/10 text-teal"}`}><Shield size={13}/>{admin?"Admin":"Staff"}</span></td></tr>})}</tbody></table></div>}</div></div>}